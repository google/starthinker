Congratulations on launching StarThinker in a Google Cloud shell.
  
The repository code has already been installed, from here just execute the recipe.

Please type the following command and hit the enter key:

pip install -r requirements.txt
source setup.sh
python all/run.py gtech/say_hello.json --verbose

You will need to upload the recipe you plan to run, and execute it just like the sample.
