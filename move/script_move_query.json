{
  "script":{
    "icon":"arrow_forward",
    "product":"BigQuery",
    "title":"Query To Table",
    "description":"Move data from a Query or PLX into a BigQuery table.",
    "instructions":[
      "To query PLX use: SELECT * FROM [plx.google:FULL_TABLE_NAME.all] WHERE...",
      "The query will only be created once, to update it, visit the dataset and remove the existing query."
    ],
    "authors":["kenjora@google.com"]
  },
  "move":{
    "auth":"user",
    "from":{
      "useLegacySql":{"field":{ "name":"move_useLegacySql", "kind":"boolean", "order":1, "default":true }},
      "query":{"field":{ "name":"move_query", "kind":"text", "order":2, "default":"" }}
    },
    "to":{
      "dataset":{"field":{ "name":"move_dataset", "kind":"string", "order":3, "default":"" }},
      "table":{"field":{ "name":"move_table", "kind":"string", "order":4, "default":"" }}
    }
  }
}
